from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small ml project where how we restore our data',
    author='ravish',
    license='',
)
